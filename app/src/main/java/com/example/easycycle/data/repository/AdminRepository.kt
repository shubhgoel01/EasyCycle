package com.example.easycycle.data.repository

import com.example.easycycle.data.model.Student
import com.example.easycycle.data.remote.StudentFirebaseService

/*
class AdminRepository
{
    private val studentFirebaseService: StudentFirebaseService=StudentFirebaseService()
    suspend fun addStudent(student: Student) {
        studentFirebaseService.addStudent(student)
    }

    suspend fun studentExists(student: Student): Boolean {
        return studentFirebaseService.studentExist(student)
    }
}
*/

